// fallow-ignore-file unused-file -- framework convention: config auto-discovered by nitro
import { defineConfig } from "nitro"

export default defineConfig({
  traceDeps: ["i18n-iso-countries*"],
})
